type Mutation{
    addBook(book: BookInput): Book
    updateBook(book: BookInput): Book
    deleteBook(isn: String): String
}

input BookInput{
    isn: String
    name: String
    publisher: String
    author: String
    publishedDate: String
}